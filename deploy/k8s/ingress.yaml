# 艺本艺术 Ingress 配置
# 用于外部访问 API、官网和 H5 应用

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shiyiju-ingress
  namespace: shiyiju
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
spec:
  rules:
    # API 网关
    - host: api.shiyiju.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 80
            # 限流注释
    # 官网根域名
    - host: a.art1.cn
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
    - host: art1.cn
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
    - host: www.art1.cn
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
    # 管理后台
    - host: admin.art1.cn
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: admin
                port:
                  number: 80

---
# 前端 Service (官网 + H5 应用共用)
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: shiyiju
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: frontend

---
# 前端 Deployment (官网 + H5 应用共用)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: shiyiju
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: shiyiju/frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"

---
# 管理后台前端 Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-frontend
  namespace: shiyiju
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-frontend
  template:
    metadata:
      labels:
        app: admin-frontend
    spec:
      containers:
        - name: admin-frontend
          image: shiyiju/admin-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"

---
# 管理后台前端 Service
apiVersion: v1
kind: Service
metadata:
  name: admin-frontend
  namespace: shiyiju
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: admin-frontend
