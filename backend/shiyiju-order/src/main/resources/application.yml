server:
  port: 8083
spring:
  application:
    name: shiyiju-order
  cloud:
    nacos:
      discovery:
        enabled: false
        server-addr: 127.0.0.1:8848
  datasource:
    url: jdbc:mysql://localhost:3306/shiyiju_local?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0

# 微信支付配置
wxpay:
  # 公众号 AppID（H5/公众号 JSAPI 支付）
  app-id: ${WXPAY_APP_ID:wx_your_app_id}
  official-app-id: ${WXPAY_OFFICIAL_APP_ID:${WXPAY_APP_ID:wx_your_app_id}}
  # 小程序AppID（用于JSAPI支付，与appId相同）
  mini-app-id: ${WXPAY_MINI_APP_ID:wx_your_app_id}
  # 商户号（在微信支付商户平台获取）
  mch-id: ${WXPAY_MCH_ID:your_mch_id}
  # 兼容旧字段，V2 签名优先读取 api-key，未配置时回退到 mch-key
  mch-key: ${WXPAY_MCH_KEY:your_mch_key}
  # 商户API证书路径（退款等操作需要，apiclient_cert.p12）
  key-path: ${WXPAY_KEY_PATH:}
  # V2 API密钥（商户平台 -> 账户中心 -> API安全）
  api-key: ${WXPAY_API_KEY:your_api_key}
  # 支付回调通知地址（必须为外网可访问的 HTTPS 地址，且需直达网关 /pay/callback/**）
  notify-url: ${WXPAY_NOTIFY_URL:https://a.art1.cn/pay/callback/notify}
  # 交易类型: JSAPI(小程序/公众号) / NATIVE(扫码) / APP(APP支付)
  trade-type: JSAPI
  # API版本: v2 或 v3
  version: v2
  # V3 证书序列号
  serial-no: ${WXPAY_SERIAL_NO:}

# 支付宝支付配置
alipay:
  enabled: ${ALIPAY_ENABLED:false}
  app-id: ${ALIPAY_APP_ID:}
  private-key: ${ALIPAY_PRIVATE_KEY:}
  alipay-public-key: ${ALIPAY_PUBLIC_KEY:}
  gateway-url: ${ALIPAY_GATEWAY_URL:https://openapi.alipay.com/gateway.do}
  notify-url: ${ALIPAY_NOTIFY_URL:https://a.art1.cn/pay/alipay/notify}
  return-url: ${ALIPAY_RETURN_URL:https://a.art1.cn/#/pages/order/list}
  sign-type: RSA2
  charset: UTF-8

wallet:
  service-url: ${WALLET_SERVICE_URL:http://127.0.0.1:8081}

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.shiyiju.order.entity
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    com.shiyiju: DEBUG
