# app.json 页面注册示例

将以下页面加入小程序 app.json：

```json
{
  "pages": [
    "pages/artist-profile/artist-profile",
    "pages/artist-score/artist-score",
    "pages/artwork-detail/artwork-detail"
  ]
}
```

如果已有 app.json，只需要把这三个路径加入 pages 数组即可。
