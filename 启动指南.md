# 拾艺局后端服务启动指南

## 一、环境状态

### ✅ 已配置完成 (2026-04-20)
- ✅ Java 17
- ✅ MySQL 8.0 (root/root123456)
- ✅ Redis 服务
- ✅ shiyiju 数据库及所有表
- ✅ Nacos 注册中心
- ✅ 所有微服务已启动

---

## 二、一键启动

### 方式一：使用一键脚本（推荐）

```bash
cd /Users/master/CodeBuddy/art12

# 一键启动所有服务
./scripts/start-all.sh

# 查看服务健康状态
./scripts/health-check.sh

# 一键停止所有服务
./scripts/stop-all.sh
```

### 方式二：手动启动

```bash
cd /Users/master/CodeBuddy/art12

# 1. 启动Nacos
cd nacos && sh bin/startup.sh -m standalone &
sleep 15

# 2. 启动微服务
cd ../backend
for jar in shiyiju-gateway shiyiju-user shiyiju-product shiyiju-order \
           shiyiju-auction shiyiju-promotion shiyiju-community \
           shiyiju-file shiyiju-message shiyiju-admin; do
    java -jar ${jar}/target/${jar}-1.0.0-SNAPSHOT.jar > /tmp/${jar}.log 2>&1 &
done

# 3. 启动前端
cd ../frontend && npm run dev:h5 &
cd ../admin && npm run dev &
```

---

## 三、服务状态

| 端口 | 服务 | 说明 | 状态 |
|------|------|------|------|
| 8848 | Nacos | 注册中心 | ✅ |
| 8080 | Gateway | API网关 | ✅ |
| 8081 | User | 用户服务 | ✅ |
| 8082 | Product | 商品服务 | ✅ |
| 8083 | Order | 订单服务 | ✅ |
| 8084 | Auction | 拍卖服务 | ✅ |
| 8085 | Promotion | 分销服务 | ✅ |
| 8086 | Community | 社区服务 | ✅ |
| 8087 | File | 文件服务 | ✅ |
| 8088 | Message | 消息服务 | ✅ |
| 8090 | Admin | 管理后台 | ✅ |

---

## 四、控制台地址

- **Nacos**: http://localhost:8848/nacos (nacos/nacos)
- **管理后台**: http://localhost:5174 (admin/admin123)

---

## 五、API 测试

```bash
# 测试网关 - 商品分类
curl "http://localhost:8080/api/product/categories"

# 测试网关 - 订单列表
curl "http://localhost:8080/api/order/orders" -H "X-User-Id: 1"

# 测试网关 - 艺荐官
curl "http://localhost:8080/api/promotion/center" -H "X-User-Id: 1"

# 测试管理后台 - 登录
curl -X POST "http://localhost:8090/admin/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

---

## 六、前端启动

### 小程序前端
```bash
cd frontend
npm install
npm run dev:h5
# 访问 http://localhost:5173
```

### 管理后台
```bash
cd admin
npm install
npm run dev
# 访问 http://localhost:5174
```

---

## 七、微信支付配置

推荐通过环境变量配置 `backend/shiyiju-order`，不要把商户号、API Key 直接写进仓库：

```yaml
wxpay:
  app-id: ${WXPAY_APP_ID}
  mini-app-id: ${WXPAY_MINI_APP_ID:${WXPAY_APP_ID}}
  mch-id: ${WXPAY_MCH_ID}
  mch-key: ${WXPAY_MCH_KEY}
  api-key: ${WXPAY_API_KEY:${WXPAY_MCH_KEY}}
  notify-url: ${WXPAY_NOTIFY_URL}
```

示例：

```bash
export WXPAY_APP_ID=wx_xxx
export WXPAY_MINI_APP_ID=wx_xxx
export WXPAY_MCH_ID=your_mch_id
export WXPAY_MCH_KEY=your_v2_key
export WXPAY_API_KEY=your_v2_key
export WXPAY_NOTIFY_URL=https://your-domain/pay/callback/notify
```

注意：

- 微信小程序 JSAPI 支付请填写小程序 `AppID`，仓库中的 `mini-app-id` 会优先用于 JSAPI 下单和前端调起支付签名。
- 回调地址必须是微信服务器可访问的公网 HTTPS 地址。
- 当前网关放行的是 `/pay/callback/**`，所以回调地址应为 `https://你的域名/pay/callback/notify`，不要写成 `/api/order/...`。

---

## 八、数据库连接

```bash
mysql -u root -proot123456
USE shiyiju;
SHOW TABLES;
```

---

## 九、日志查看

```bash
# 查看网关日志
tail -f /tmp/shiyiju-gateway.log

# 查看订单服务日志
tail -f /tmp/shiyiju-order.log

# 查看Nacos日志
tail -f /Users/master/CodeBuddy/art12/nacos/logs/nacos.log
```

---

## 十、容器化部署

### Docker Compose 本地部署
```bash
cd deploy/docker
docker-compose up -d
```

### Kubernetes 生产部署
```bash
kubectl apply -f deploy/k8s/
kubectl get pods -n shiyiju
```

详见 `deploy/README.md`
